# Eclipse Launcher UI Parity Matrix

This ledger contains exactly 132 normalized rows from the completed clean-room audit of the legacy reference. Status describes required disposition; the first implementation milestone is recorded below without treating partial UI work as a completed row. Retained capabilities are being specified and rebuilt independently rather than copied.

| ID | Screen | Element/state | Required behavior | Material 3 implementation | Eclipse-specific change | Test/verification | Status |
|---|---|---|---|---|---|---|---|
| A01 | Bootstrap | Startup checklist and continue action | Check prerequisites, show aggregate and item status, permit continuation only when safe, recover from missing prerequisites | Full-screen `Scaffold`, `TopAppBar`, `LinearProgressIndicator`, list items, and `FilledButton` | Use original Eclipse branding, explicit aggregate progress, cancellation policy, and scoped-storage behavior | Instrumentation test for ready, running, failed, rotated, and process-restored states; accessibility order test | replaced |
| A02 | Prerequisite failure | Missing storage terminal state | Explain why startup cannot continue and provide a recoverable path without broad storage access | Centered empty/error state with `Icon`, supporting text, and recovery `Button` | Replace legacy storage prerequisite with app/instance-scoped storage or system document recovery | Test on API 26, current API, no external-storage grant, rotation, and Back | replaced |
| A03 | Launcher host | Top app bar, nested content, global tasks, current profile | Preserve the recognizable shell: title at start, Downloads and Settings at end, current route, and global progress; provide predictable Back without introducing a new permanent bottom bar | `Scaffold`, `TopAppBar`, `NavHost`, and persistent progress surface | Keep Home as root, retain trailing action order, use original branding, no remote notice, and restored route state | Navigation/Back instrumentation; action-order screenshot; process-death/rotation tests; screen-reader landmark order | replaced |
| A04 | Launcher crash | Fatal error and recovery screen | Show safe diagnostics, share support data, restart, and acknowledge safely | Scrollable error `Scaffold`, selectable diagnostic text, `OutlinedButton`, and `Button` | Redact secrets and personal data; use Eclipse support/export policy | Test stack/cause rendering, share redaction, restart, Back, rotation, and font scale 200% | replaced |
| A05 | Game exit | Process error and recovery screen | Explain signal or exit code, prohibit screenshots where required, and offer safe recovery | `Scaffold`, status `Icon`, diagnostic text, and recovery actions | Keep game process attribution and support export separate from launcher crashes | Test normal exit, signal exit, zero-code handling, secure-window state, rotation, and Back | replaced |
| A06 | Legacy extras | Hidden source-specific easter egg | Do not ship non-product or source-specific hidden experiences | None | Remove the entire route, drawable, navigation, and tests tied to it | Repository and packaged-APK scan confirms no easter-egg route or asset | intentionally removed |
| A07 | Background errors | Background error host and diagnostic dialog | Surface errors arriving without a foreground activity, support copy/details, and dismiss cleanly | Lifecycle-aware internal error destination opened by an app-owned explicit intent; no exported external deep link | Replace legacy identity, deduplicate notifications, validate the internal target, and avoid leaking stack data | Background-process, explicit-intent validation, notification tap, rotation, copy, and dismissal tests | replaced |
| A08 | Custom controls | Standalone touch-layout editor host | Edit named layouts, guard unsaved work, and support Back-to-save/discard/cancel | Full-screen `Scaffold`, `ModalNavigationDrawer` or side sheet, `IconButton`, and editor surfaces | Clean-room editor with accessible non-drag alternatives and no source-specific assets | Test create, edit, resize alternatives, save, discard, process restoration, Back, and TalkBack | replaced |
| A09 | Legacy Java runner | Standalone arbitrary JAR execution host | Not offered by Eclipse | None | Remove arbitrary JAR execution, Java/AWT runner, helper controls, and its entry points | Repository and APK scan finds no runner activity, manifest entry, or external JAR-launch intent | intentionally removed |
| A10 | Game session | Render surface, controls, hotbar, monitor, logger, and input preview | Run landscape, preserve game input, expose launcher controls, handle Back, and restore orientation safely | Full-screen Android `SurfaceView`/`TextureView` host with Compose overlay, side sheet, and dialogs | Rebuilt controls, scoped logging, accessible alternatives, and no arbitrary plugin execution | Instrumented game smoke test, orientation, Back, input, logger, monitor, process, and accessibility tests | replaced |
| F01 | Home | Management actions, account, selected instance, launch, shortcuts, and diagnostics | Preserve the observable 65–68/30–35 two-pane relationship when width permits: management actions left; account above version and Launch on right; Launch anchored near bottom | Adaptive `Scaffold` with two tonal panes or the documented compact vertical order, large `ListItem` actions, `FilledButton`, and progress | Replace JAR execution with scoped instance/content management; preserve order and hierarchy with original visuals and explicit states | Light/dark compact/landscape/tablet screenshots; all account/instance/task/launch states; TalkBack order | replaced |
| F02 | Accounts | Account list, provider actions, refresh, and removal | Support offline local accounts immediately and official Microsoft accounts after client registration; select, refresh where valid, and remove with confirmation | `LazyColumn`, `ListItem`, `RadioButton`, extended FAB/add action, and delete `AlertDialog` | Remove third-party provider tabs and credentials; disable the Microsoft add action with an explanation until DQ-016 clears | Unit/UI tests for local states and empty/disabled dependency state; removal/refresh semantics; OAuth tests after registration | replaced |
| F03 | Official authentication | Microsoft OAuth browser flow | Use approved system/custom-tab flow, preserve local accounts, and recover on cancel/failure | Custom tabs plus `Scaffold` progress/retry states and account-result deep link | Eclipse-owned approved client and policy; no embedded source WebView | Redirect, cancel, denial, process-restoration, and account-save instrumentation after client registration | blocked by dependency |
| F04 | Settings | Category rail and paged settings shell | Preserve category order and adjacent content relationship; use a rail on medium/expanded and ordered tabs/drawer on compact; no experimental destination | `TopAppBar`, `NavigationRail` or compact ordered tabs, `NavHost`, and category destinations | Keep rendering, controls, game/runtime, launcher order; add approved categories only after the reference category | Navigation, Back, category-state, rail/tab screenshots, font-scale, compact, tablet, foldable, and restoration tests | replaced |
| F05 | Settings | Video, renderer, driver, cutout, and performance | Show compatible options; explain unsupported hardware; no unsigned executable plugin flow | `ListItem`, `Switch`, `Slider`, exposed dropdown, `Card`, and `AlertDialog` | Built-in renderers only until executable plugins receive explicit security and licensing approval | API/device capability tests; unsupported-state semantics; no executable package path | replaced |
| F06 | Settings | Touch, cursor, gyro, gestures, and gamepad controls | Configure essential Android controls with dependent-state explanation and accessible alternatives | `ListItem`, `Switch`, `Slider`, `RadioButton`, and adaptive settings navigation | Original controls, original cursor artwork, and non-drag gamepad alternatives | Setting persistence, sensor absence, accessibility actions, rotation, and process restoration | replaced |
| F07 | Settings | Version, language, runtime, memory, and sandbox | Configure launch safely, warn or cap invalid memory, and manage runtimes | `ListItem`, `Switch`, `Slider`, `ExposedDropdownMenuBox`, text field, and `AlertDialog` | Remove experimental defaults; use scoped runtime policy and explicit compatibility | Range/validation, free-memory, 32-bit/API, runtime-absence, font-scale, and restoration tests | replaced |
| F08 | Settings | Download, appearance, logs, update, cache, and notifications | Configure launcher behavior with restart-pending and permission semantics | `ListItem`, `Switch`, `Slider`, color controls, and `Snackbar` | Original theming, no page-opacity legibility hazards, contextual notification rationale | Theme, font scale, contrast, permission denial, restart, cache, and process-restoration tests | replaced |
| F09 | Settings | Experimental defaults page | Not present in release navigation | None | Remove the page and all release default bindings; future work requires a separate opt-in build | Navigation, resources, settings-store, and APK scans find no experimental page | intentionally removed |
| F10 | Personalization | Custom launcher background manager | Choose app, editor, or game background target; import, preview, reset, and show explicit empty state | `TabRow`, `LazyColumn`, `AsyncImage`, empty state, `IconButton`, and `FilledTonalButton` | Use original visual design and app-scoped validated images only | Import, invalid image, preview, reset, empty, rotation, Back, and process-restoration tests | replaced |
| F11 | Personalization | Custom virtual cursor manager | Import and select a cursor, show preview/default, and validate image use | `LazyColumn`, `ListItem`, `AsyncImage`, empty state, and `IconButton` | Original cursor assets and no unrestricted file path | Import, invalid file, selection, preview update, empty, Back, and restoration tests | replaced |
| F12 | Controls | Control-layout manager and selector | List, search, create, import, copy, paste, refresh, and choose a default layout | `SearchBar`, `ListItem`, `TopAppBar`, FAB/menu, `AlertDialog`, and bottom sheet | Scoped app storage, safe JSON validation, accessible selection, and no executable content | Create/import/validation/search/empty/paste/permission/process tests | replaced |
| F13 | Controls | Physical gamepad remapping | Capture mappings in menu/game contexts with keyboard and switch-access alternatives | `LazyColumn`, `RadioButton`, mode selector, focus management, and confirmation dialogs | Map through accessible named actions rather than touch-only behavior | Hardware, no-hardware, capture timeout, cancel, Back, and accessibility tests | replaced |
| F14 | Content | Five-category content hub | Preserve mod, mod-pack, resource-pack, save, and shader category order and adjacent content; local/import actions remain usable while network categories are dependency-blocked | `NavigationRail` on medium/expanded, ordered compact tabs/drawer, `Scaffold`, and `NavHost` | Bind each network action only to an approved documented source; show explicit unavailable state until dependency clears | Category/order, local import, Back, offline, source-policy, rotation, and process-restoration tests | blocked by dependency |
| F15 | Content | Mod catalog and filters | Search, paginate, filter, and import approved mod metadata/files after a source adapter is approved | `SearchBar`, filter `BottomSheet`, `LazyColumn`, loading/empty states, and dependency-unavailable content | Per-source API/terms decision, no analytics, cached-state labeling, instance-confined execution, and safe dependency handling | Local import plus source-approved search/filter/pagination/offline/empty/failure/cancel/process tests | blocked by dependency |
| F16 | Content | Mod-pack catalog and local import | Browse approved packs and safely import local packs with dependency confirmation and instance scope | Filter sheet, `ListItem`, `AlertDialog`, and global progress | Remote source is dependency-blocked; executable content is confined to the selected instance, integrity/source is shown, and no arbitrary launcher path/plugin execution is allowed | Local import/transaction tests now; source, compatibility, duplicate, offline, dependency, cancel, and restore tests after approval | blocked by dependency |
| F17 | Content | Resource-pack catalog and import | Browse approved sources and import compatible resource packs into the selected instance | Filter sheet, `LazyColumn`, import action, progress, and dependency-unavailable content | Scoped instance storage and source/attribution display; remote source blocked pending approval | Local import/invalid archive/conflict/progress/restoration tests now; search/offline/source tests after approval | blocked by dependency |
| F18 | Content | Save/world catalog and import | Browse approved sources and safely import/unpack saves into scoped storage | Filter sheet, `ListItem`, progress, failure content, and dependency-unavailable remote state | Validate archive paths/symlinks and prevent traversal; remote source blocked pending approval | Local Zip-slip, invalid archive, duplicate, import, cancel, and restore tests now; source/offline tests after approval | blocked by dependency |
| F19 | Content | Shader-pack catalog and import | Browse approved sources and install compatible shader packs | Filter sheet, `LazyColumn`, import action, progress, and dependency-unavailable remote state | Scoped instance storage and source/attribution; remote source blocked pending approval | Local import/validation/progress/restoration tests now; source/search/offline tests after approval | blocked by dependency |
| F20 | Content | Project detail, versions, and screenshots | Show approved-source metadata, versions, dependencies, screenshots, and safe instance-confined install; Back unwinds child list | `TopAppBar`, `AsyncImage`, `LazyColumn`, retry, and child navigation | Remote details remain dependency-blocked until source/API terms are approved; no launcher-process code execution or silent errors | Local metadata/import tests now; detail, screenshot, child Back, dependency, task, offline, and restore tests after source approval | blocked by dependency |
| F21 | Installation | OptiFine version selector | Show only versions from an approved documented source that are compatible with the selected base version | `TopAppBar`, `RadioButton`, loading/empty/error states, confirmation, and dependency-unavailable content | Source/terms/redistribution compatibility must clear DQ-015 before enabling; accessibility remains required | Parser/unit tests now; source, compatibility, empty, offline, selection, Back, and restore tests after approval | blocked by dependency |
| F22 | Installation | Forge version selector | Show compatible releases from approved documented sources and installation state | `TopAppBar`, radio list, progress, error, and dependency-unavailable states | Source/terms/install-process review must clear DQ-015; no launcher-process plugin execution | Version model tests now; source, compatibility, offline, install, Back, and restore tests after approval | blocked by dependency |
| F23 | Installation | NeoForge version selector | Show compatible releases from approved documented sources and installation state | `TopAppBar`, radio list, progress, error, and dependency-unavailable states | Source/terms/install-process review must clear DQ-015; no launcher-process plugin execution | Version model tests now; source, compatibility, offline, install, Back, and restore tests after approval | blocked by dependency |
| F24 | Installation | Fabric loader selector | Show compatible releases from approved documented sources and installation state | `TopAppBar`, radio list, progress, error, and dependency-unavailable states | Source/terms/install-process review must clear DQ-015; executable content stays inside the selected instance | Version model tests now; source, compatibility, offline, install, Back, and restore tests after approval | blocked by dependency |
| F25 | Installation | Fabric API selector | Show compatible API versions from an approved source and remove/add state | `TopAppBar`, radio list, progress, dependency, and dependency-unavailable states | Source/terms approval is required; executable API files remain confined to the selected game instance | Dependency model tests now; compatibility, offline, selection, remove, install, Back, and restore tests after approval | blocked by dependency |
| F26 | Installation | Quilt loader selector | Show compatible releases from approved documented sources and installation state | `TopAppBar`, radio list, progress, error, and dependency-unavailable states | Explicit source/terms/install commitment must clear DQ-015; executable content stays in the selected instance | Version model tests now; compatibility, offline, install, Back, and restore tests after approval | blocked by dependency |
| F27 | Installation | Quilt support-library selector | Show compatible library versions from an approved source and their dependency relationship | `TopAppBar`, radio list, progress, dependency, and dependency-unavailable states | Source/terms approval is required and files remain confined to the selected instance | Dependency model tests now; compatibility, offline, install, remove, Back, and restore tests after approval | blocked by dependency |
| F28 | Versions | Installed-version list, favorites, and instance paths | Select, install, create path, favorite, refresh, and show loading/empty states | `SearchBar`, tabs, `ListItem`, FAB, `CircularProgressIndicator`, and delete sheet | Scoped instance storage, accessible favorites, and explicit empty/install CTA | Select, favorite, delete, refresh, empty, permission, Back, rotation, and restore tests | replaced |
| F29 | Versions | Base-game version selector | Filter by channel, search versions, and open installation safely | `TabRow`, search field, `LazyColumn`, loading and empty states | Approved official source and offline-cached labeling | Channel/filter/search/empty/offline/failure/selection/process tests | replaced |
| F30 | Installation | Instance name and add-on composer | Preserve input, show add-on states, validate name, block incompatibility, and start installation | `TextField`, `AssistChip` or selectable cards, compatibility `AlertDialog`, and `Button` | Original compatibility UI, no Java-runner path, durable draft restoration | Name validation, duplicate, all add-on states, compatibility warning, process death, and Back tests | replaced |
| F31 | Version details | Instance and content shortcuts plus rename/copy/delete | Open scoped content, configure, rename, copy, or remove with safe confirmation | `ListItem`, grouped cards, `AlertDialog`, and progress | No unrestricted file path; all operations are instance-scoped | No-instance, every shortcut, rename/copy validation, delete, offline, Back, and restore tests | replaced |
| F32 | Version details | Per-instance configuration | Configure isolation, path, controls, rendering, runtime, metadata, and icon with unsaved protection | Scrollable form with `ListItem`, `Switch`, dropdowns, text fields, and save bar | Scoped paths only, original renderer list, no plugins, accessible dependent-state rules | Save/cancel/unsaved Back, disabled path, icon validation, rotation, and process tests | replaced |
| F33 | Content | Scoped instance file/content manager | Browse, search, import, create folder, select, copy, move, rename, share, and delete only managed content | `SearchBar`, `NavigationDrawer` or tree, `ListItem`, `FloatingActionButton`, and action sheet | Replace unrestricted file manager and broad storage with scoped instance manager | Scoping, invalid URI, import, search, multi-select, share, rename, delete, empty, Back, and restore tests | replaced |
| F34 | Content | Instance mod manager | Enable, disable, import, search, bulk-manage, and explain empty state | `ListItem`, `Checkbox`, search, `Snackbar`, and confirmation dialog | Safe JAR parsing without executing content; scoped instance only | Extension validation, enable/disable, bulk, import, search, empty, permission, and restore tests | replaced |
| F35 | About | Product information and support entry | Show original Eclipse identity, version/legal information, and a disabled explanatory support row until an owned endpoint exists | `Scaffold`, `TopAppBar`, about sections, and `ListItem` | Remove legacy support/community identity; never hide About because support provisioning is pending | Identity/legal snapshot, unavailable support state, Back, rotation, and localization tests | replaced |
| F36 | About | Legal, license, and attribution page | Display Apache-2.0 and required upstream attributions with accessible external-link actions | Scrollable `ListItem` content and confirmation bottom sheet for external links | Original Eclipse legal copy and only required approved references | Legal completeness, link labels, unavailable handler, Back, font scale, and localization tests | replaced |
| F37 | About | Sponsor/funding page | Not part of Eclipse product scope | None | Remove sponsor page, service calls, data models, resources, and navigation | Repository, dependency, network-policy, and APK scans find no sponsor surface or endpoint | intentionally removed |
| D01 | Shared dialogs | Generic tip, confirm, warning, checkbox, selectable message | Present title, body, actions, optional checkbox, and warning semantics with predictable Back | `AlertDialog` or `BasicAlertDialog`, `Checkbox`, scrollable body, and `Snackbar` for brief status | Standardize destructive, validation, countdown, and action ordering; original copy only | Semantics, focus order, Back, checkbox, countdown, long message, and font-scale tests | replaced |
| D02 | Shared dialogs | Destructive confirmation | Identify object and consequences; safe cancel/default first | `AlertDialog` with `TextButton` cancel and error-colored `Button` confirm | Require confirmation for all destructive operations, including custom-control deletion | Every destructive route, object naming, Back, outside-tap policy, and accessibility action-order tests | replaced |
| D03 | Shared dialogs | Generic text input | Label, message, optional checkbox, input, validation, and preserved draft | `AlertDialog`, `OutlinedTextField`, `Checkbox`, and supporting/error text | Replace legacy generic form with type-specific accessible dialogs | Required, numeric, duplicate, illegal-character, IME, rotation, and process-restoration tests | replaced |
| D04 | Shared progress | Blocking task-running dialog | Announce task, prevent conflicting actions, and expose cancel only when safe | `AlertDialog` with `CircularProgressIndicator` and cancel action | Replace indefinite modal with named, cancellable, or explicitly non-cancellable task policy | Task start/update/end announcements, cancel policy, Back, rotation, and process tests | replaced |
| D05 | Content operations | File copy/move/delete progress | Show bytes, count, rate, progress, and cancel before destructive commit | `AlertDialog`, `LinearProgressIndicator`, live status, and cancel `Button` | Scoped operations only; explicit partial-cancel semantics | Large file, empty set, rate, cancel, failure, rotation, and process-restoration tests | replaced |
| D06 | Updates | App update download progress | Show source, version, size, progress, and cancel once owned update metadata exists | `AlertDialog` with determinate progress, cancel, and retry state | Block until Eclipse-owned update endpoint and package-distribution decision exist | Metadata, offline, size, resume, cancel, integrity, permission, and failure tests | blocked by dependency |
| D07 | Shared errors | Standard error dialog with expandable trace and copy | Present concise cause, optional details, copy, and safe recovery | `AlertDialog` or dedicated error content with expandable section and copy `Button` | Redact secrets and replace raw exception copy with structured support export where possible | Short/long error, copy, expand, redaction, forced-exit, Back, rotation, and font-scale tests | replaced |
| D08 | Versions | Game-version picker | Filter by channel, search, select, and return without duplicate route growth | Full-screen dialog or `ModalBottomSheet` with `TabRow` and `SearchBar` | Use compact sheet and expanded dialog/side-sheet variants | Compact/expanded, all channels, search, no match, Back, rotation, and restoration tests | replaced |
| D09 | Runtime | Java-runtime picker | List compatible runtimes and automatic option; explain absence | `ModalBottomSheet` or dialog with `RadioGroup` | No legacy runtime identity or direct archive execution | Compatible/incompatible/no-runtime, selection, Back, rotation, and process tests | replaced |
| D10 | Accounts | External-provider account form | Not offered by Eclipse | None | Remove all non-Microsoft provider forms, server lists, credentials, and service calls | APK, source, dependency, and network scan finds no third-party auth endpoint or form | intentionally removed |
| D11 | Accounts | External-provider role chooser | Not offered by Eclipse | None | Remove role-selection route tied to external providers | Repository and navigation scan finds no external role dialog | intentionally removed |
| D12 | Content operations | File/content action panel | Offer only valid copy, move, share, rename, delete, select, and edit actions | `ModalBottomSheet` with `ListItem` actions and safe cancel | Use scoped item IDs, URI grants, and no unrestricted path | Single/multi, folder, selected item, disabled actions, Back, and screen-reader tests | replaced |
| D13 | Content operations | Delete confirmation for one or many items | Name or count, consequences, safe default, and progress handoff | `AlertDialog` with destructive `Button` | Enforce for account, instance, content, cache, and editor deletion | Every delete source, multi-item wording, cancel, Back, and process tests | replaced |
| D14 | Content dependencies | Dependency review and install | Show sorted dependencies, required/optional state, source, integrity/compatibility, and consequences | Full-screen adaptive dialog with `ListItem` and install/cancel actions | Remote dependencies require approved source/terms; game code is installed only into the selected instance and never into the launcher process | Required/optional, conflict, offline, cancel, install, Back, and restoration tests | blocked by dependency |
| D15 | Custom controls | Control-layout selector | Search/list managed layouts and return selected layout | `ModalBottomSheet` or dialog with `SearchBar` and `ListItem` | Scoped validated layouts and accessible empty state | Empty, search, invalid file, selection, Back, rotation, and restoration tests | replaced |
| D16 | Custom controls | Virtual-cursor selector | List default and approved custom cursors with preview | `ModalBottomSheet`, `ListItem`, and `AsyncImage` | Original assets and instance-safe validation | Empty, default, invalid image, selection, Back, and restoration tests | replaced |
| D17 | Legacy plugins | Executable renderer-plugin manager | Not offered without a separately approved secure plugin architecture | None | Remove unsigned executable plugin import, execution, storage, and management | APK/source scan finds no plugin execution API, APK import, or plugin-management surface | intentionally removed |
| D18 | Custom controls | Control-layout metadata viewer | Show original metadata with close and edit | `AlertDialog` or adaptive sheet with `ListItem` | Original metadata labels and no source identity | Unknown-value, edit, close, Back, rotation, and font-scale tests | replaced |
| D19 | Custom controls | Control-layout metadata editor | Required filename and optional metadata with collision validation | `AlertDialog`, `OutlinedTextField`, and `Checkbox` where needed | Original metadata model and scoped file naming | Empty, duplicate, illegal name, save, cancel, rotation, and restoration tests | replaced |
| D20 | Custom controls | Single virtual-key picker | Select one key with immediate confirmation and accessible keyboard navigation | Full-screen adaptive dialog with virtual keyboard and search/filter | Original key mapping; no automatic capture without explicit focus | All categories, search, Back, keyboard navigation, rotation, and restoration tests | replaced |
| D21 | Custom controls | Multi-key picker | Select a combination, show count, clear, confirm, and cancel | Full-screen adaptive dialog with selectable keys and selected chips | Clean-room combination mapping and accessible ordered announcements | Select/deselect/clear/confirm/cancel, keyboard, Back, and restoration tests | replaced |
| D22 | Updates | App update details and actions | Show approved metadata, notes, size, state, update, skip, and cancel | Adaptive dialog with scrollable release notes and three explicit actions | Block content/actions until Eclipse-owned update endpoint exists | Metadata, no notes, ignore, offline, update, cancel, and rotation tests | blocked by dependency |
| D23 | Versions | Favorite-folder selector | Multi-select categories, persist, and expose empty state | `ModalBottomSheet` or dialog with `Checkbox` and empty state | Keep only if version organization remains approved in Eclipse | Create, select, persist, empty, close, rotation, and restoration tests | replaced |
| D24 | Runtime | Multiple-runtime manager | List, import, validate, rename, configure, and remove runtimes with destructive confirmation | `Scaffold`, `LazyColumn`, import action, and `AlertDialog` | Scoped runtime storage, no arbitrary archive path, original UI | Import, invalid runtime, compatibility, rename, remove, empty, Back, and restore tests | replaced |
| D25 | Search | Floating file-search panel | Query, search, case sensitivity, results-only, and result count | `SearchBar` in sheet or docked responsive search container | Replace floating window with adaptive Material search pattern | Open/close, focus, IME, hits, zero, filters, Back, rotation, and restoration tests | replaced |
| D26 | Menus | Single-action long-press popup | Never be sole route; expose action from visible overflow or selection UI | `DropdownMenu` or contextual action sheet | Move delete/edit/default actions to discoverable controls | Touch and TalkBack access, keyboard access, Back, and action invocation tests | replaced |
| D27 | Custom controls | Editor side drawer | Add controls, load/save/export, snapping, default, and guarded exit | `ModalNavigationDrawer` or adaptive side sheet with `ListItem` controls | Clean-room editor, no executable content, accessible layout | All actions, unsaved exit, drawer Back, rotation, and process-restoration tests | replaced |
| D28 | Custom controls | Selected-control property panel | Edit name, size, mappings, behavior, appearance, and visibility with type-specific fields | Full-screen route or adaptive sheet with form controls and `Slider` | Original model and accessible numeric alternatives | Each control type, conditional fields, invalid values, Back, rotation, and restore tests | replaced |
| D29 | Custom controls | Color selection panel | Select hue, saturation/value, optional alpha, preview, and validate hex input | Full-screen color route with custom accessible controls plus standard text field | Original accessibility semantics and contrast safeguards | Hue/alpha disabled, valid/invalid hex, preview, Back, rotation, and restore tests | replaced |
| D30 | Custom controls | Contextual action row and resize handle | Delete, clone, add-sub, and resize with accessible alternatives | Floating toolbar plus `Slider` or increment/decrement actions | Add delete confirmation and non-drag resize controls | All control types, action visibility, keyboard alternatives, Back, and restoration tests | replaced |
| D31 | Onboarding | First-use spotlight sequences | Explain essential actions once, allow skip/replay, and never block required use | Material onboarding guidance or accessible `Dialog` sequence | Replace legacy tours; no sponsor/news content; original copy | First run, skip, replay, rotation, process death, Back, and TalkBack order | replaced |
| D32 | News | Remote announcement card | Not part of Eclipse product scope | None | Remove remote news/social card, service, links, drag behavior, and tests | Source, dependency, network, resource, and APK scans find no announcement surface | intentionally removed |
| D33 | Global progress | Task count and expanded task details | Survive navigation, show named tasks/progress, and allow safe cancellation | Persistent progress `Surface` with expandable sheet and `LinearProgressIndicator` | Original task model and accessible announcements | Zero, one, many, completion, failure, cancel, rotation, Back, and process tests | replaced |
| D34 | Game session | In-game settings drawer | Control logs, force close, custom keys, monitor, scale, input, gyro, hotbar, and layouts | End-side `ModalNavigationDrawer` or sheet over game surface | Remove plugin actions; provide accessible named actions and confirm force close | Open/close, every retained action, Back/game Escape, rotation, and process tests | replaced |
| D35 | Game session | Floating memory/FPS monitor | Show selected metrics, placement, opacity, and menu action | Floating `Surface` with metric text and settings link | Legibility-safe opacity; no data collection or telemetry | Metric toggles, placement, opacity, hidden states, game lifecycle, and accessibility tests | replaced |
| D36 | Game session | Log overlay | Toggle logs, view, copy, clear with confirmation, and close | Full-screen `Scaffold` with selectable `LazyColumn` and app bar actions | Scoped support logs with privacy controls and no telemetry | Toggle, copy, clear, confirm, large log, Back, rotation, and process tests | replaced |
| D37 | Game session | IME input preview | Show current input while IME is visible and hide without focus loss | Compose overlay tied to IME insets | Original styling and no capture or logging of input | Show/hide, multi-line input, IME resize, rotation, and screen-reader tests | replaced |
| D38 | Game session | Startup/version tip | Briefly show version metadata without obstructing play | Snackbar or dismissible overlay | Short, skippable, original feedback; no splash branding | Delayed display, auto-dismiss, skip, rotation, and game readiness tests | replaced |
| D39 | Game session | Buttons, drawers, joysticks, touchpad, and hotbar | Deliver input, toggle states, and expose accessible alternatives | Android game surface with overlay and accessible control/action route | Original controls; non-touch alternatives; no arbitrary executable behavior | Press/hold/toggle/drawer/joystick/hotbar, visibility, Back, rotation, and accessibility tests | replaced |
| D40 | Legacy Java runner | Touch helper controls for arbitrary Java | Not offered by Eclipse | None | Remove Java-runner mouse, movement, keyboard, copy/paste, and virtual-mouse actions | APK and source scan finds no Java-runner helper control | intentionally removed |
| D41 | Legacy Java runner | Java-runner log overlay | Not offered by Eclipse | None | Remove Java-runner log surface and entry | APK and source scan finds no Java-runner logger | intentionally removed |
| D42 | Storage permission | Legacy external-storage permission | Not used by Eclipse | None | Remove old storage permission request and manifest declarations; use scoped storage and system documents | API 26–29 permission scan and storage test prove no legacy request | intentionally removed |
| D43 | Storage permission | Broad all-files-access settings | Not used by Eclipse | None | Remove all-files rationale, settings intent, and manifest permission | Manifest/code scan finds no broad storage request; scoped import tests pass | intentionally removed |
| D44 | Notifications | Notification rationale | Explain user benefit immediately before a real notification feature request | `AlertDialog` or education flow tied to the feature action | Request only when needed; support denied and permanent-denial states | First request, rationale skip, grant, denial, permanent denial, settings, and rotation tests | replaced |
| D45 | Permissions | Notification OS permission | Request at point of use, preserve work, and explain permanent denial | Android permission launcher and explanatory UI | No background or launch-time surprise request | API 32/33+ grant, denial, one-time, permanent denial, and restart tests | replaced |
| D46 | Content import | System document picker and creation | Select/create allowed content, preserve URI grant, and return or cancel safely | `ActivityResultContracts.OpenDocument` or `CreateDocument` | Scoped destinations and MIME validation; no unrestricted path | Grant, denial, cancel, invalid MIME, multiple selection, rotation, and process grant tests | replaced |
| D47 | Updates | Package installation handoff | Not available until owned update distribution is approved | None | Block package-installer flow pending approved update endpoint and signing policy | Manifest/code scan has no update installer until dependency clears; then signed-APK tests | blocked by dependency |
| D48 | External links | Browser and support/share destinations | Confirm external navigation, handle missing handler, and use temporary share grants | Confirmation sheet plus Android intents and `ActivityResultContracts` | Support destination blocked until approved endpoint; all other links use allowlist | No handler, cancel, browser return, share grant expiry, rotation, and process tests | blocked by dependency |
| D49 | Background errors | Error notification and deep link | Notify once, preserve diagnostic safety, and open recoverable error UI | Notification channel and deep link to error activity | Original notification identity and deduplication; no sensitive stack data | Background error, duplicate suppression, tap, rotation, dismiss, and privacy tests | replaced |
| S01 | Bootstrap state | Preflight disabled | Continue is non-clickable while prerequisites are unresolved and exposes busy semantics | Disabled `Button`, `CircularProgressIndicator`, and status semantics | Aggregate progress and safe reason announcement | Enabled-state assertion, TalkBack announcement, Back, and process tests | replaced |
| S02 | Bootstrap state | Component queued | Each component has explicit queued/waiting state | `ListItem` with status text and queued indicator | Aggregate queue and no indistinguishable idle state | All queued, mixed, empty, rotation, and accessibility tests | replaced |
| S03 | Bootstrap state | Component running | Indeterminate item spinner and running label | `CircularProgressIndicator` in `ListItem` | No jump to completion without announcement | Start, long run, rotation, process recreation, and live-region test | replaced |
| S04 | Bootstrap state | Components complete | Show all complete once and transition safely | Completed check state and enabled continue | Original aggregate completion feedback | Completion count, duplicate navigation prevention, Back, and restoration tests | replaced |
| S05 | Prerequisite state | Missing storage | Route to scoped-storage recovery without broad access | Blocking empty/error destination | Use app/instance storage or system document migration | No-storage, recovery, rotation, Back, and API 26 tests | replaced |
| S06 | Account state | No accounts | Show explicit empty state with official and local add actions | Empty-state layout with two `Button` actions | No third-party provider; OAuth action dependency-blocked | Initial empty, cancel OAuth, local add, rotation, and TalkBack order tests | replaced |
| S07 | Account state | Current account present | Show avatar, name, provider, and switch/remove affordances | `ListItem`, `RadioButton`, overflow menu, and avatar | Original avatar handling and no credential display | Current selection, switch, remove, local account, and screen-reader tests | replaced |
| S08 | Instance state | No installed instance | Show explicit install/content-create CTA and block Launch with reason | Empty state with primary `Button` and disabled launch `Button` | No blank version label or hidden shortcut | Initial empty, create, cancel, process, and accessibility tests | replaced |
| S09 | Instance state | Instance selected | Show icon, name, metadata, and enabled Launch | Selected `ListItem` or hero card with `FilledButton` | Original metadata and no source-specific visual treatment | Select/change instance, missing file recovery, launch, rotation, and TalkBack tests | replaced |
| S10 | Task state | Active task blocks conflicting action | Preserve current screen, show task owner, and explain blocked action | Global progress and inline supporting state | Replace shake-only feedback with accessible reason and link to tasks | Launch/install/import block, multiple tasks, completion, Back, and announcement tests | replaced |
| S11 | Version state | Version refresh loading | Keep context visible, expose progress, and avoid duplicate refresh | Refresh `LinearProgressIndicator` with disabled refresh | Cached-versus-live labeling and safe retry | Refresh, rotate, back, process, and accessibility state tests | replaced |
| S12 | Version state | No installed versions | Explain absence and offer install action | Empty state with install `Button` | No blank list or implicit catalog dependency | Empty after removal, initial install, offline cached list, Back, and focus test | replaced |
| S13 | Collection state | Empty file/content/layout lists | Explain scope and provide relevant create/import/catalog action | Empty state with contextual `Button` | Use scoped instance manager and no broad file route | Empty for every collection, action, Back, rotation, and screen-reader test | replaced |
| S14 | Search state | File search has matches | Highlight and announce result count | `SearchBar`, highlighted `ListItem`, and polite status | Original search behavior and instance scope | Hits, highlighted semantics, no stale count, Back, and restoration tests | replaced |
| S15 | Search state | File search has zero matches | Distinguish no matches from empty collection and offer reset | Filtered-empty state with reset `Button` | Preserve query and explain active scope | Zero results, clear query, filters, rotation, and TalkBack test | replaced |
| S16 | Content state | Multi-select active | Show selected count, select all, and valid bulk actions | `Checkbox`, selection app bar, and action sheet | Preserve query context and announce count | Select/deselect/all/cancel, invalid parent rows, Back, and restoration tests | replaced |
| S17 | Content state | Folder-selection mode | Change confirm action, hide import if invalid, and return scoped ID | Full-screen picker with selected path and confirm `Button` | Never emit unrestricted filesystem path | Select, confirm, cancel, Back, rotation, invalid path, and restoration tests | replaced |
| S18 | Add-on state | Not selected | Show available action and disabled removal control | Selectable card with trailing disabled icon | Explain optional addition and compatibility | Every add-on unselected state and accessibility role test | replaced |
| S19 | Add-on state | Selected | Show selected version, type, and enabled remove action | Selected card with check/remove semantics | Announce version and dependency relationship | Select/remove/replace, process restore, and TalkBack selected-state test | replaced |
| S20 | Add-on state | Incompatible and disabled | Explain conflicting selection and prevent navigation | Disabled card/list item with supporting reason | Original compatibility reasons and no shake-only feedback | Every compatible/incompatible matrix, removal recovery, and accessibility test | replaced |
| S21 | Catalog state | Initial loading | Center or inline loading with cancellation where safe | `CircularProgressIndicator` and status text | Distinguish network, service, and source unavailable | Slow, cancel, offline, retry, rotation, and process tests | replaced |
| S22 | Catalog state | Results and pagination | Show result cards, cache age, and footer progress | `LazyColumn` with pagination progress | Approved sources only and no analytics | Initial/load-more/end, stale request, offline, and restoration tests | replaced |
| S23 | Catalog state | No results | Explain filters/source and offer reset or alternative source | Filtered-empty state with reset actions | Preserve query and selected filters | Zero result, reset, source switch, offline, Back, and focus tests | replaced |
| S24 | Catalog state | Offline or service failure | Distinguish offline, unsupported source, internal error, and retry | Persistent offline banner plus error state | Never collapse offline into no result; no telemetry | Network toggles, timeout, server error, unsupported source, retry, and announcements | replaced |
| S25 | Detail state | Detail versions loading | Keep metadata visible and label loading list | Inline `LinearProgressIndicator` | Preserve previous detail during refresh | Cold load, refresh, offline, cancel, rotation, and TalkBack tests | replaced |
| S26 | Detail state | No versions or failed load | Explain cause and provide retry or return | Error empty state with retry and Back | Technical exceptions are not user-facing copy | Empty, offline, failed, retry, Back, and process tests | replaced |
| S27 | Detail state | Child version list | Show child title and Back restores parent without duplicate stack | Nested route and `TopAppBar` Up action | Preserve filters and parent scroll when possible | Child open/Back/process death, rotation, and scroll restoration tests | replaced |
| S28 | Screenshot state | Image loading | Show per-image loading without blocking metadata | `AsyncImage` placeholder/progress | Avoid layout jump and announce completion once | Success, slow, cache hit, rotation, and screen-reader test | replaced |
| S29 | Screenshot state | Image failure and retry | Show textual failure and labeled retry action | Error placeholder with `IconButton` and tooltip/content description | No icon-only retry and no silent failure | Failed request, retry, offline, rotation, and accessibility test | replaced |
| S30 | Settings state | Unsupported hardware/API | Keep row visible with disabled reason or hide only behind explanation | Disabled `ListItem` with supporting text | No silent disappearance; no Vulkan/gyro assumptions | API/device matrix, no-sensor, screen-reader, and layout tests | replaced |
| S31 | Settings state | Parent-dependent controls | Show dependencies and update enabled/visible states consistently | `Switch`, `Slider`, and `AnimatedVisibility` | Avoid layout jumps and preserve values | Each parent toggle, disabled child, process restoration, and accessibility test | replaced |
| S32 | Runtime state | Memory allocation exceeds free memory | Warn, constrain, or require confirmation with current values | `Slider`, numeric field, warning banner/dialog | Base cap on available device memory rather than warning only | Low-memory, boundary, 32-bit, API, rotation, and validation tests | replaced |
| S33 | Control state | No custom layouts | Explain feature and offer create/import if approved | Empty state with create/import `Button` | Scoped validated layouts only | Empty, create, import, failure, Back, and restoration tests | replaced |
| S34 | Control state | Editor modified and unsaved | Distinguish saved, modified, and error states; guard Back/restart | Editor app bar status and discard/save dialogs | Original draft model and no immediate destructive delete | Modify/save/discard/Back/process death/rotation and TalkBack tests | replaced |
| S35 | Legacy sponsor state | Sponsor loading, success, or hidden failure | Not part of Eclipse product scope | None | Remove sponsor models, service, UI, resources, and tests | Repository, dependency, resource, and APK scans find no sponsor state | intentionally removed |
| S36 | Appearance state | Light, dark, dynamic color, background, and metric previews | Maintain readable contrast and restore theme/preview state | Material 3 color roles and `isSystemInDarkTheme` | Original Eclipse theme; no opacity-dependent unreadability | Light/dark/dynamic, contrast, font scale, background, rotation, and restoration tests | replaced |

## Phase 2 evidence

The first target-owned UI milestone is intentionally not counted as a completed row:

- `:core:designsystem` centralizes Material 3 theme roles, dimensions, typography, shapes, original vector geometry, and reusable components.
- `:feature:home` provides the wide 6.5/3 and compact prerequisite-state layouts, with no permanent bottom navigation.
- The empty state exposes no simulated account, instance, task, launch, or navigation result. Disabled actions carry an explanation and no enabled action can fall through to a no-op.
- Previews cover wide light/dark, compact, dynamic color, and enlarged text. Actions runs `36040467149` passed the connected Compose tests, including a screenshot theme contract; run `36040467221` passed unit tests, lint, debug assembly, and APK inspection.
- F01 and related state rows remain `replaced` until their full behavior, repository wiring, restoration, and device verification are complete.

## Phase 3 evidence

The navigation milestone is also intentionally not counted as a completed parity row:

- `:app` owns a single Navigation Compose graph with Home as root and Downloads/Settings as real sibling destinations.
- Top-level actions preserve title-leading and Downloads/Settings-trailing order without a permanent bottom navigation bar.
- Downloads and Settings expose saveable category selection with adaptive tabs/rails and explicit empty or unavailable states; no remote source, fake progress, or simulated repository result is present.
- Actions run `36043991071` passed the app navigation/Back test and all Compose tests; run `36043991257` passed unit tests, lint, debug assembly, and APK inspection.
- A03, F04, F28, F35, and remaining destination rows remain `replaced` until their full behavior and verification contracts are complete.
